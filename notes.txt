
todo:
- make constant in css for animate time for active fade

- info page section (horizontal group plus:)
    - has the bottom margin 

- info page section group
    - fixed gap (which they all share and is variable)
    - border debugging

- horizontal group
    - horizontal flex
    - flex centered

- vertical group
    - vert flex
    - flex start

- grid group
    - defined width and height (ignores element width)
    - items centered when last row does not meet col count

- info page element:
    - width (value, percent, or empty for 100%)

- info page img
    - can have default img width
    - takes in an ordered ref list that adds the image props it is showing, and for being able to show fullscreen view
    - fullscreen view for images and grid gallery
        - load a page overlay
        - arrows for going from one image to next (only if more than one)

- info page text
    - default aligned left like normal

- info page video


<title>Alejandro Hervella</title>
<nav>about, games, etc  (no h1, h2, h3)
<h1> about me</h1>



TODO NEXT:
- for claude code and adding a back button at each info page (wip)

I want to do a bit of refactoring with my button component structure.

Currently I have this hierarchy of buttons:
- Base Button component
    - has expandable parameters for button basics: href, button events, basic CSS than can be expanded upon
- Nav Button component extends Base Button component:
    - adds concept that button is being used for navigating to somewhere (external site or internal page) and whether it's external or not to properly config on the Inside

- Header Nav Button component extends Nav Button component:
    - adds the use of naming the button with a lockey and adds animation


I want to refactor this setup so that instead we have:

- Base Button component stays exactly the same

- Anim Label Button component
    - extends the Base Button component
    - applies the mouse hover highlighting part and parameters from the old Nav Header Button component without the isActive selection logic portion (that goes later)

- Nav Button component
    - extends the Anim Label Button component
    - adds the parts from the old nav button that allows us to provide the urlPath and isExternalLink parameters

- Nav Selection Button component
    - now here take the logic that was allowing us to add the selecting portion from the old NavHeaderButton (the isActive logic)

In doing all of this, make sure you accomadte for new names, pair up new css files with the new components and their respective css clauses (which are already definied in the old css respective files) that are needed for the logic described in each new component




- make AnimButton,  then AnimNavButton



I would like to add a back button feature that can be reused across the different pages I plan to implement. The back button will appear just under the nav bar, and will be justified to the right.

I would like it to be it's own component that lives in the pages component folder (within its own folder to house the tsx and css file).

I want the button's component to be based classed from the NavHeaderButton class since I want it to have the same highlighting style and animation, and text in the middle. The changes the expanded features the new class will have are: no selection view since this is just a 

I want the button to be a square with a border that is the same thickness as the left and right middle lines in the nav bar. Inside the button it should say "back", and when you hover over it, the same animation and style as the nav bar button highlights appears when highlighted over it. Since these are both the same functionality, I would like to base class this further



- basic page for info, text, and embedded images or videos
    - paragraphs, headers, banners, videos, side to side images
    - eventually an image grid browser interface for art stuff, can start with most basic image browser I can find to get site up and running

    Types:
    - back button at the top on all of them (or follows the bottom)
    - title and words
    - image left and words right (and cascades switching sides if multiple images)
        - image + paragraph styles
    - Layout of images you can like and page through (description below or top)
    - full page width of image with description below or top
    - custom


- game page highighting and click animation

________________________

TODO LATER:

- find a way to get the root directory for out of folder references so that we don't break paths when I move thigns around
- nav bar:
    - nav bar highlight vs selection difference
    - highlight should go away when not hovering
    - flicker button on click effect
- page loading effect?
- shrink size of everythingdirect formatting
- game page:
    - spacing between line in the middle during narrow windows

- game idea:
    - wall jumper game? jump either in place or across to the next wall. Can slide up the longer you hold, dodge obstacles
    - highscore you can register with log in with a profile pic, something simple
    - multiplayer with friend to see each other race at the same time, also simple, just streaming inputs

________________________

LOG:

Saturday March 28th, 2026
- Got git setup with lazy git! Prettttty nice
- Added localization libraries and switched all of that over
- base classed buttons further from nav buttons for any kind of button with proper css styles

Friday March 27th, 2026
- got basically everything set up with components: made the dedicated pages, dedicated button base classes and sub classes, used react router for navigation, looking good!
- Today's song: Golpes Bajos - CLUBZ

Thursday March 28th, 2026
- Finished adding a generic info page that will be used across site items
- Fixed what I thought was the way to add sub routed pages but that's only for adding content ontop of the parent's page, not for if I want to just rerender the entier page without referencing the parent
- started using and understanding claude code's limitations and best practices, implemented with project